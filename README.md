 При `--workers=1` загрузка фактически последовательная: один воркер
обрабатывает id по очереди — пока не придёт ответ по текущему фильму, следующий
запрос не начнётся. При `--workers=10` в сети одновременно летят до 10 запросов,
и время ожидания ответа перекрывается.
